# AGENTS.md — PixFold(项目级)

> PixFold:图片整理与 CBZ 制作工作台(图形应用),目标平台 Windows / Android(优先)/ Linux。
> **当前阶段:设计期(D0~D1)。仓库内容是设计文档 + 行为参考脚本,不是可交付产品代码。**
> 本文件优先级低于用户当回合指令,高于全局 AGENTS.md;技术栈/流程/环境类全局约定见全局文件。

## 1. 文档层级(谁在哪记录什么)

| 文件 | 职责 |
|---|---|
| `HANGOFF.md` | **产品决策唯一权威**:定位、已确认约束、用户工作流 A/B/C、统一领域模型(§5)、交互原则、平台策略、技术栈决策门槛、验收标准、待确认问题 |
| `s_handoff.md` | 开发环境实况与 Android 平台验证准备(命令、版本锚点、踩坑) |
| `README.md` | 项目概览,内容须与 HANGOFF 保持一致 |
| `scripts/` | 两个 Python 脚本:现有行为参考、回归样例、命令行备用;**不是最终规范**——GUI 需补全交互并避免静默危险操作 |
| `GLOBAL-SKILLS-MANIFEST.md` | 全局技能待装清单(运维向,与产品开发无关) |
| `.workbuddy/` | WorkBuddy 会话记忆,已在 .gitignore,勿混入产品内容 |

设计期的**领域术语、决策、约束变更 → 回写 `HANGOFF.md` 对应章节**,不在别处另起炉灶;环境类变动回写 `s_handoff.md`。

## 2. 不可违背的产品约束

1. **扫描、决策、执行分离**:扫描/预览阶段不改文件;执行前必须给出完整计划供用户确认。
2. **建议 ≠ 事实**:自动识别的排序、名称、卷号、语言必须可见、可改。
3. **预览是主工作区,不是日志**。
4. **批次统一 + 逐项例外**:批量设置后允许单项覆盖。
5. **破坏性动作可解释**:默认保留源文件;删除必须独立确认并列出影响范围。
6. **Android SAF 优先验证**:目录授权、遍历、读写、缩略图、后台任务。

## 3. 领域词汇

设计/实现讨论一律用 `HANGOFF.md` §5 统一领域模型中的词。发现术语冲突或缺口时:**先与用户确认,再回写 HANGOFF**,不要自行造词或悄悄换说法。当前阶段 `HANGOFF.md` 就是领域主场;引入独立 CONTEXT.md / ADR(如经 domain-modeling skill)须用户同意。

## 4. 当前阶段:该做 / 不该做

**该做**(设计路线 D0→D1→D2,详见 HANGOFF §9):
- D0 固化需求:从两个脚本整理功能矩阵、真实/异常样例,标出必须人工确认的字段;
- D1 交互原型:图片缩略图/拖拽排序、命名结构编辑、CBZ 元数据编辑、执行计划预览(可不连真实文件系统);
- D2 平台验证:Android SAF 选目录、持久化授权、遍历/读写、重命名、缩略图、后台任务。

**不该做**:
- 不创建正式工程、不锁定技术栈(Flutter 只是候选之一;满足 HANGOFF §8 门槛 + 用户点头才动手);
- 不把 `scripts/` 现有行为当作最终规范照搬。

## 5. 脚本使用

- Python ≥3.10,PEP 723 内联依赖(metadata 在脚本头部 `# ///` 块);用 `uv run scripts/<名>.py …` 运行。
- 脚本含交互输入与真实文件操作:**运行前先向用户说明影响范围**;能 dry-run 就先 dry-run。

## 6. 其他约定

- 语言:与用户用中文交流;仓库文档双语(README)或中文(HANGOFF/s_handoff),新增/修改匹配所在文件的语言风格。
- Git:本仓库独立于 chezmoi dotfiles 仓库;`.workbuddy*/`、`.agents/` 已 gitignore,不要手动加回。
- 验收:任何改动对照 `HANGOFF.md` §10(功能 / 数据正确性 / 平台)自检。
