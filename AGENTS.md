# AGENTS.md — PixFold(项目级)

> PixFold:图片整理与 CBZ 制作工作台(图形应用),目标平台 Windows / Android(优先)/ Linux。
> **当前阶段:设计期。技术栈候选假设已定(Flutter 主候选 / CMP 对照 / Tauri 排除,§8.1),开发环境验收完成(§12),下一步为 D2 SAF spike(§8.3,可先于 D1)与 D1 交互原型。仓库内容 = 设计文档 + 行为参考脚本 + 环境记录,不是可交付产品代码。**
> 本文件优先级低于用户当回合指令,高于全局 AGENTS.md;技术栈/流程/环境类全局约定见全局文件。

## 1. 文档层级(谁在哪记录什么)

| 文件 | 职责 |
|---|---|
| `HANGOFF.md` | **产品决策唯一权威**:定位、已确认约束、用户工作流 A/B/C、统一领域模型(§5)、交互原则、平台策略(§7)、技术栈决策(§8:候选假设 8.1 / 证伪条件与顺序 8.2 / D2 SAF spike 方案 8.3)、设计路线(§9)、验收标准(§10)、待确认问题(§11)、开发环境实况与 Android 验证准备(§12,由原 s_handoff.md 并入)、参考文件(§13) |
| `README.md` | 项目概览与门面,内容须与 HANGOFF 一致;含候选栈环境部署节(⚠️ 暂定,只含项目相关步骤) |
| `.mise.toml` | 项目工具版本声明(**纯配置无注释**,说明见 HANGOFF §12.2);变更后需 `mise install` |
| `scripts/` | 两个 Python 脚本:现有行为参考、回归样例、命令行备用;**不是最终规范**——GUI 需补全交互并避免静默危险操作 |
| `GLOBAL-SKILLS-MANIFEST.md` | 全局技能待装清单(运维向,与产品开发无关) |
| `.workbuddy/` | WorkBuddy 会话记忆,已在 .gitignore,勿混入产品内容 |

设计期的**领域术语、决策、约束变更 → 回写 `HANGOFF.md` 对应章节**,不在别处另起炉灶;环境/工具类变动回写 `HANGOFF.md` §12。原 `s_handoff.md` 已于 2026-09-07 并入 §12 后删除,**勿再新建同类文件**。

## 2. 不可违背的产品约束

1. **扫描、决策、执行分离**:扫描/预览阶段不改文件;执行前必须给出完整计划供用户确认。
2. **建议 ≠ 事实**:自动识别的排序、名称、卷号、语言必须可见、可改。
3. **预览是主工作区,不是日志**。
4. **批次统一 + 逐项例外**:批量设置后允许单项覆盖。
5. **破坏性动作可解释**:默认保留源文件;删除必须独立确认并列出影响范围。
6. **Android SAF 优先验证**:目录授权、遍历、读写、缩略图、后台任务。

## 3. 领域词汇

设计/实现讨论一律用 `HANGOFF.md` §5 统一领域模型中的词。发现术语冲突或缺口时:**先与用户确认,再回写 HANGOFF**,不要自行造词或悄悄换说法。`HANGOFF.md` 是领域主场;引入独立 CONTEXT.md / ADR(如经 domain-modeling skill)须用户同意。

## 4. 当前阶段:该做 / 不该做

**该做**(路线与执行顺序以 HANGOFF §8.2 / §8.3 / §9 为准):
- 技术栈进入 spike 验证:候选假设 Flutter(主)/ Compose Multiplatform(对照)/ Tauri(暂排除),证伪条件见 §8.2;
- D2 SAF spike(§8.3,可先于 D1 完整原型):最小工程验证 SAF 闭环;临时工程放**仓库外** `C:\Personal\pixfold-spike`,不污染仓库;
- D1 交互原型(技术栈经 spike 确认后启动):图片缩略图/拖拽排序、命名结构编辑、CBZ 元数据编辑、执行计划预览,可不连真实文件系统;
- 环境与工具维护:版本声明经 `.mise.toml`(项目)与全局配置(chezmoi 纳管,见 §12.2);Windows 命令行统一敲 `flutter`(pwsh 适配,git-bash/CI 用 `flutter.bat`,§12.5)。

**不该做**:
- 不创建正式工程、不把候选假设当锁定(栈变更须命中 §8.2 证伪条件并经用户确认);
- 不把 `scripts/` 现有行为当作最终规范照搬;
- 不用 `mise use -g` 改全局工具声明(绕过 chezmoi 造成漂移)。

## 5. 脚本使用

- Python ≥3.10,PEP 723 内联依赖(metadata 在脚本头部 `# ///` 块);用 `uv run scripts/<名>.py …` 运行。
- 脚本含交互输入与真实文件操作:**运行前先向用户说明影响范围**;能 dry-run 就先 dry-run。

## 6. 其他约定

- 语言:与用户用中文交流;仓库文档双语(README)或中文(HANGOFF),新增/修改匹配所在文件的语言风格。
- Git:本仓库独立于 chezmoi dotfiles 仓库(当前工作分支 `ready`);`.workbuddy*/`、`.agents/` 已 gitignore,不要手动加回。
- 验收:任何改动对照 `HANGOFF.md` §10(功能 / 数据正确性 / 平台)自检。
